/*
 * Copyright © 2016-2022 Iury Braun
 * Copyright © 2017-2022 Neo7i
 */

package go_mongodb_dao

import (
	//"github.com/pkg/errors"
	//db "github.com/romvarac/buuuk-interview/database"
	//"go.mongodb.org/mongo-driver/mongo/gridfs"
	//"go.mongodb.org/mongo-driver/mongo/options"
)

/*
// GetBucket is define
func GetBucket(name string, conn *db.Connection) (*gridfs.Bucket, error) {
	err := conn.Connect()

	if err != nil {
		return nil, errors.Wrap(err, "get bucket fail")
	}

	return gridfs.NewBucket(conn.Client.Database(db.Database), options.GridFSBucket().SetName(name))
}

func (m *Dao) GetBucket(name string) (*gridfs.Bucket, error) {
	return gridfs.NewBucket(client.Database(m.Database), options.GridFSBucket().SetName(name))
}
*/
